return{
    {
        "mbbill/undotree",
        cmd = "UndotreeToggle", -- optional: loads only when this command is used
        keys = {
            { "<leader>u", "<cmd>UndotreeToggle<CR>", desc = "Toggle Undotree" }
        }
    }
}
