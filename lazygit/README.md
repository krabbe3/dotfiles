# Lazygit

Lazygit configuration — terminal UI for Git.

## Structure

```
lazygit/
└── .config/lazygit/
    └── config.yml    # Lazygit config
```

## Notes

- Installed by `setup_full.sh` (v0.53.0 binary to `~/.local/bin/lazygit`)
- Config file present for future customization (currently uses lazygit defaults)
